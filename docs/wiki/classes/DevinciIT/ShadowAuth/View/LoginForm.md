# `DevinciIT\\ShadowAuth\\View\\LoginForm`

Concrete form schema for login submissions.

## Required Fields

- `username` (`text`, required)
- `password` (`password`, required)

Inherits rendering and CSRF behavior from `BaseForm`.
